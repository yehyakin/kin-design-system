# Generated Agent distribution

Status: generated, non-normative delivery surface

`next/` is a deterministic compact derivative of the current declared repository inputs. It is committed for offline inspection and drift review, but Phase 1 does not publish it to GitHub Pages and does not provide a stable compatibility promise.

Do not edit generated files. Change the normative contract or the declared non-normative inputs under `distribution/`, then run:

```powershell
npm run agent:export
npm run agent:check
```

No version archive, stable alias, registry, or component Recipe artifact exists in Phase 1. Locale manifests remain incomplete until the exact English and Simplified Chinese copy receives checksum-bound human review.
